# 项目总设计方向：基于 Agentic AI 的卡车司机连续找货决策

最后更新：2026-06-07 00:25 +08:00

本次更新：切换至官方 2026-05-29 数据版本。公开司机偏好已发生结构性变化，旧版确定性基线和逐司机调参不再可外推。最终路线改为“运行时偏好编译器 + 通用约束日程器 + 罚款感知 MPC + 稀疏 Qwen 解析”，详细执行要求见 `最终冲刺与泛化方案.md`。

## 0. 新版本设计原则

正式评测使用未公开司机，保持高分的关键不是记住 D001/D002，而是把运行时偏好文本编译成统一约束并持续追踪完成度。

最终系统分四层：

1. **Preference Compiler**：正则/规则优先，Qwen 仅补充低置信解析，输出严格 `ConstraintSpec`。
2. **Constraint Ledger**：根据历史维护每日休息、静默窗口、整日停驶、区域接单日、到访/停留和事件路线进度。
3. **Schedule-Aware MPC**：对接单、等待、空驶投影罚款变化和未来任务可达性。
4. **Profit Optimizer**：只在合法可行候选内比较即时净收益、单位时间收益、目的地机会价值与查询成本。

公开样本中的城市名、货物名只能作为运行时参数，不能出现在司机专用代码中。对无法解释的高罚款规则必须保守处理并触发一次结构化模型解析，不能静默忽略。

## 1. 项目目标

本项目要实现一个可独立运行的司机找货智能体，在官方 `demo` 仿真环境中，针对每位司机从 `2026-03-01 00:00:00` 开始推进完整自然月经营决策。每一步只能选择 `take_order`、`wait`、`reposition` 三类动作之一，目标是在遵守赛题约束和司机偏好的前提下提升月度净收益。

核心不是单笔订单预测，而是事件驱动、多步连续经营决策。策略必须考虑当前位置、当前时间、货源有效期、装货时间窗、空驶成本、运输耗时、后续可观测机会和司机个性化规则。

## 2. 已准备数据与代码

- 当前官方压缩包：`demo_docs_release_20260529.zip`；旧 `20260509` 版本仅作历史参考。
- 官方工程已解压到：`demo/`。
- 官方文档已解压到：`docs/`。
- 公开数据位于：`demo/server/data/cargo_dataset.jsonl` 与 `demo/server/data/drivers.json`。
- 本地数据规模：货源 500,000 条、31 个品类；公开司机 2 名，正式评测另有未公开司机。

注意：这些数据只能用于理解字段和本地评测。Agent 决策代码在仿真运行中禁止直接读取上述原始文件，也禁止复制后从其他路径直读。

## 3. 必须严格遵守的赛题要求

1. 决策入口保持官方接口：`ModelDecisionService.decide(driver_id) -> dict`。
2. 决策代码只能通过 `SimulationApiPort` 访问环境：
   - `get_driver_status(driver_id)`
   - `query_cargo(driver_id, latitude, longitude)`
   - `query_decision_history(driver_id, step)`
   - `model_chat_completion(payload)`
3. 禁止在决策中打开、解析、扫描或缓存 `cargo_dataset.jsonl`、`drivers.json` 及同内容文件。
4. 禁止按具体 `driver_id` 写死规则。偏好可以通过运行时返回的 `preferences` 文本解析成规则，但不能写 `if driver_id == "D009"` 这类逻辑。
5. 每步动作只允许：
   - `{"action": "take_order", "params": {"cargo_id": "..."} }`
   - `{"action": "wait", "params": {"duration_minutes": 正整数} }`
   - `{"action": "reposition", "params": {"latitude": 数值, "longitude": 数值} }`
6. `query_cargo` 会消耗仿真时间：默认最多返回 100 条，浏览批大小 10，因此返回 100 条会消耗 10 分钟。策略不能无限查询。
7. 正式评测会重新运行官方服务端和收益脚本，提交包不应包含 `demo/server/data/`。
8. 复赛约束：总仿真运行时长不超过 4 小时，每个司机 token 消耗不超过 500 万。

## 4. 官方仿真与评分口径

### 4.1 时间和距离

- 仿真时间单位为分钟。
- 仿真起点为 `2026-03-01 00:00:00`。
- 空驶与接单赴装货点距离使用 Haversine 大圆距离，单位 km。
- 空驶速度来自配置 `reposition_speed_km_per_hour`，demo 默认 60 km/h。
- 距离换时间：`ceil(distance_km / speed * 60)`，最少 1 分钟；但接单赴装货点距离接近 0 时为 0 分钟。

### 4.2 收益公式

每位司机：

```text
gross_income = 成功接单货源价格总和
distance_km = 空驶里程 + 成功接单干线里程
cost = distance_km * cost_per_km
net_income = gross_income - cost - preference_penalty
```

API 返回的候选货源中 `price` 已被官方 demo 转为元；收益脚本会用原始数据回查并按 `/100` 转元。

### 4.3 接单流程

接单动作会依次发生：

1. 从当前点空驶到货源 `start`。
2. 若早于 `load_time` 开始，则等待到开始时间。
3. 若晚于 `load_time` 结束，则接单失败且货源下架。
4. 运输 `cost_time_minutes` 分钟，完成后位置变为货源 `end`。
5. 若完单超过仿真月度上界，收益可能不计入。

## 5. 推荐实现架构

后续实现主要修改 `demo/agent/`，尽量不改 `demo/server/` 与 `demo/simkit/`。如必须增加辅助文件，放在 `demo/agent/` 内，并在 `demo/agent/README.md` 或 `demo/SUBMISSION.md` 说明。

建议模块：

- `model_decision_service.py`：保留官方入口，负责调用 Planner 并兜底输出合法动作。
- `planner.py`：核心滚动规划器，完成状态读取、候选生成、打分、动作选择。
- `preference_rules.py`：把 `preferences` 文本解析成可执行规则，并计算候选动作风险。
- `state_tracker.py`：从 `query_decision_history` 与本轮内部记忆重建司机累计状态。
- `geo.py`：Haversine、时间换算、区域/圆形禁入判断。
- `schemas.py`：内部候选动作与评分结果的数据结构。
- `llm_helper.py`：仅在必要时调用模型，例如偏好文本结构化和打分解释，不让模型直接随意输出动作。

整体采用“确定性规划为主，Qwen3.5-Flash 辅助理解偏好”的混合策略。这样更稳、更省 token，也更容易通过动作合法性校验；同时与复赛推荐基座模型保持一致。

## 6. 核心算法路线

### 6.1 滚动时域规划

每步决策使用 Model Predictive Control 思路：

1. 获取当前司机状态。
2. 查询当前位置附近候选货源。
3. 生成合法接单候选、休息候选、空驶候选。
4. 对每个候选估计本步净收益、时间占用、偏好风险和后续机会价值。
5. 选择综合评分最高且风险可控的动作。
6. 每步重新观察环境并重算，不依赖一次性全月静态计划。

### 6.2 接单候选评分

对每条货源计算：

```text
pickup_km = haversine(current, cargo.start)
pickup_minutes = 0 if pickup_km <= 1e-6 else ceil(pickup_km / speed * 60)
arrival_minutes = now + pickup_minutes
wait_for_load = max(0, load_start - arrival_minutes)
finish_minutes = max(arrival_minutes, load_start) + cost_time_minutes
haul_km = haversine(cargo.start, cargo.end)
gross = cargo.price
travel_cost = (pickup_km + haul_km) * driver.cost_per_km
base_net = gross - travel_cost
```

过滤规则：

- 货源车型不匹配则过滤。
- 到达时间晚于装货窗结束则过滤。
- 完单时间超过月度仿真上界则强降权或过滤。
- 命中高罚分或无封顶偏好风险则过滤。
- 接单动作起点或行驶区间落入“禁止接单/禁止空驶”的时间窗则过滤。
- 违反城市边界、禁入圆区、最大空驶、最大干线距离等规则则过滤。

评分建议：

```text
score =
  base_net
  + 0.25 * net_per_hour
  + destination_opportunity_bonus
  + preference_progress_bonus
  - waiting_penalty
  - deadline_risk_penalty
  - preference_violation_penalty
  - excessive_deadhead_penalty
```

其中 `net_per_hour = base_net / max(1, total_minutes / 60)`。权重先用可解释常量，后续根据本地评测结果小步调参。

### 6.3 后续机会价值

不允许读取全量货源文件，因此后续机会价值只能来自运行时观察与轻量先验：

- 记录每次 `query_cargo` 返回的候选目的地、起点城市、价格、有效时间。
- 维护按区域和小时段聚合的 `market_heat`：候选数、均价、平均净收益、平均等待时间。
- 对候选订单的终点，根据已观察 `market_heat` 给 `destination_opportunity_bonus`。
- 当观察不足时，使用保守策略：优先去已观测货源密度高、空驶成本低、且不冲突偏好的区域。

不要把公开数据统计硬编码进 Agent；公共数据和复赛隐藏数据可能不同。

### 6.4 休息策略

休息不是兜底等待，而是偏好管理和低谷避险工具。生成候选：

- `wait(30)`、`wait(60)`、`wait(120)`：普通低收益或无货场景。
- 等到下一个整点、早高峰、装货窗开始前的短休。
- 为满足连续休息偏好，生成 `wait(180/240/300/480)` 等规则需要的完整休息段。
- 夜间禁行或“禁止接单/空驶”窗口前，直接休息到窗口结束。
- 整天不出车偏好要在月初或低收益日主动安排，不要等月末被动补。

休息评分：

```text
wait_score =
  preference_progress_bonus
  + avoided_violation_penalty
  + expected_market_recovery
  - opportunity_cost
```

若所有接单候选风险高或净收益低，休息往往优于盲目空驶。

### 6.5 空驶策略

空驶只在“当前位置候选质量差且目标区域预期收益足以覆盖空驶成本”时使用。

候选目标来源：

- 当前查询结果中高分货源的装货点附近。
- 过去观察到高 `market_heat` 的区域中心。
- 司机偏好要求的家、目标点、接人点、允许边界内安全点。
- 避开禁入区或越界后的最近安全点。

可选的有限前瞻：

- 每若干小时最多做一次远端 `query_cargo` 探测，查询 1 到 3 个候选目标即可。
- 每次探测都会消耗仿真时间，必须把 `query_scan_cost_minutes` 计入评分。
- 如果当前附近已有高质量订单，不做额外探测。

空驶评分：

```text
reposition_score =
  expected_best_net_after_move
  - deadhead_km * cost_per_km
  - move_time_opportunity_cost
  - preference_violation_penalty
```

没有足够证据时，空驶评分应偏保守。

## 7. 偏好规则处理方案

偏好是得分关键，建议实现“文本解析 + 规则对象 + 动作模拟校验”。

### 7.1 解析方式

1. 先用正则识别高频规则：
   - 不接某货源品类。
   - 每日连续休息 N 小时。
   - 某时间段不接单、不空驶。
   - 自然月至少 N 个整天不接单或不空驶。
   - 单笔干线距离不得超过 N km。
   - 赴装货点空驶不得超过 N km。
   - 累计空驶不得超过 N km。
   - 每天接单不得超过 N 单。
   - 首单开工不得晚于指定时间。
   - 必须到达某坐标、必须在家、禁入圆区、市域边界。
   - 指定熟货源必须接。
2. 对无法识别的偏好，只调用一次 LLM 结构化，缓存结果。
3. 结构化结果必须保留原文、罚金额度、封顶信息和适用时间窗。

### 7.2 规则对象

每条规则实现两个方法：

```python
is_hard_constraint() -> bool
estimate_penalty(action_candidate, state) -> float
progress_bonus(action_candidate, state) -> float
```

高罚分、无封顶、长期连锁影响的规则视为硬约束，例如禁入区、夜间禁止出车、家事约定、指定熟货。低罚分且收益明显覆盖时可作为软约束，但要明确扣分。

### 7.3 状态重建

优先使用 `query_decision_history(driver_id, step)` 获取历史动作，结合当前状态维护：

- 每日接单数。
- 每日连续休息最长时长。
- 每日是否完全不出车。
- 夜间/午间禁行动作区间。
- 累计空驶里程。
- 已到达目标点的自然日集合。
- 家事/熟货等一次性任务完成状态。

不要通过读取 `demo/results/` 来恢复历史。

## 8. LLM 使用策略

基座模型明确选用 `qwen3.5-flash`。建议把 Qwen3.5-Flash 用在“偏好文本结构化、歧义解释、少量候选裁决”，不要把全量候选直接交给模型自由发挥。

推荐调用场景：

- 每个司机首次出现时，把 `preferences` 结构化成规则 JSON。
- 当确定性评分前 3 名分差很小，且偏好文本有歧义时，让模型做一次裁决。
- 异常兜底时，让模型在少量候选中选择合法动作。

工程约定：

- 所有模型请求都通过官方 `SimulationApiPort.model_chat_completion`。
- 请求体显式写入 `model: "qwen3.5-flash"`，避免误用其他模型。
- 没有 API key、模型超时或返回格式错误时，必须无损降级到确定性策略。
- 本地默认不开启模型调用；如需启用，使用 `AGENT_ENABLE_QWEN35_FLASH=1`。

不推荐：

- 每一步把 100 条货源原样塞进模型。
- 让模型输出未经本地校验的动作。
- 使用长链路推理文本；输出必须是 JSON。

最终动作返回前必须经过本地校验器，保证字段、类型和参数合法。

## 9. 兜底策略

任何异常都不能让仿真崩掉。建议兜底顺序：

1. 若存在合法且正收益明显的候选，选择最高分 `take_order`。
2. 若当前处于禁行动作窗口或需要补休，返回对应 `wait`。
3. 若有明确安全目标且空驶距离短，返回 `reposition`。
4. 否则返回 `wait(60)`；月底或临近窗口时缩短为 `wait(30)`。

兜底动作也要检查不违反硬规则。

## 10. 已完成基础实现与后续步骤

已完成：

1. `demo/agent/geo.py`、`preference_rules.py`、`state_tracker.py`、`planner.py` 已建立。
2. `model_decision_service.py` 已保留官方入口，并提供异常兜底。
3. Haversine、时间窗、装货窗、收益估算、偏好解析、接单候选排序、等待和空驶候选均已实现。
4. Qwen3.5-Flash 的 `preference_hints`、`rank_cargos`、`suggest_decision` 与 `apply_qwen_hints` 已在当前分支接入主流程，默认不开启。
5. 无模型完整 31 天仿真已能跑通，无 `validation_error`，无失败司机。

后续步骤：

1. 用真实 API key 验证 `AGENT_ENABLE_QWEN35_FLASH=1`，先短测再完整 31 天。
2. 对比不开模型与启用 Qwen 的净收入、罚分、token、耗时和模型调用次数。
3. 继续定位 D009 home-night、D010 家事/必访点、连续休息等剩余罚分。
4. 保持小步调参，避免一次性大改。

## 11. 本地验证流程

在 `demo/server` 下复制配置：

```powershell
Copy-Item config\config.example.json config\config.json
```

通过环境变量设置模型密钥，避免写入仓库：

```powershell
$env:DASHSCOPE_API_KEY = "你的密钥"
```

短跑调试：

```powershell
cd D:\竞赛\demo\server
python main.py --max-steps 200
```

收益校验：

```powershell
cd D:\竞赛\demo
$env:PYTHONIOENCODING = "utf-8"
python calc_monthly_income.py
```

最终本地评测建议把 `simulation_duration_days` 调为 31，完整跑自然月。每次检查：

- `demo/results/actions_202603_*.jsonl` 非空。
- `run_summary_202603.json` 完整。
- `monthly_income_202603.json` 中没有 `validation_error`。
- `summary` 的 `failed_drivers` 为空或原因明确。
- `preference_penalty` 与各司机规则明细可解释。

## 12. 提交边界

官方提交 ZIP 以 `demo/` 为根目录。

初赛：

- 至少包含 `demo/agent/`。
- 按当期要求可能需要包含 `demo/results/`。
- 不提交 `demo/server/data/`。

复赛：

- 至少包含 `demo/agent/`。
- 不提交 `demo/results/`。
- 不提交数据。

如果实现修改了 `simkit/`、增加了额外依赖或替换模型网关，必须在 `demo/SUBMISSION.md` 写明文件清单、用途、加载方式和与官方 demo 的差异。

## 13. 审阅关注点

后续实现完成后，审阅时优先检查：

1. 是否违反数据直读限制。
2. 是否按 driver_id 硬编码偏好或行为。
3. 动作输出是否总是合法 JSON。
4. 是否可能因模型异常导致仿真中断。
5. 是否过度查询货源导致时间被查询消耗。
6. 休息、夜间禁行、家事、熟货、禁入区等高罚分规则是否被优先满足。
7. 收益估算是否把空驶、干线、等待和装货窗都算进去。
8. 完整月仿真是否有校验失败、超时或 token 失控。

## 14. 对实现模型的简短指令

请在严格遵守官方接口和数据访问限制的前提下，保留确定性可解释 Planner 作为安全底座，并把 Qwen3.5-Flash 作为受控增强层。不要追求花哨多智能体框架；先保证合法、稳健、低罚分、可复现，再让模型处理偏好文本歧义、货源评分和少量候选复审。每次改动后都运行官方仿真与收益脚本，用结果文件驱动下一轮调参。

## 15. 当前实施状态（截至 2026-06-06 00:00 +08:00）

本项目已完成确定性滚动规划 Agent，并把 `qwen3.5-flash` 接入到偏好增强和候选复审路径。当前代码位于 `demo/agent/`，官方入口、数据访问边界和无模型降级能力均已保留。默认不开启模型；设置 `AGENT_ENABLE_QWEN35_FLASH=1` 后才会调用 Qwen。

当前工作分支：`deepseek/third-round-optimization`。本轮完成了：6h 休息触发窗口、安静窗口感知休息截止、home-night 跨日 deadline 修正、Qwen 全面集成测试与结论记录。

### 确定性基线结果（无 Qwen，2026-06-05）

| 司机 | 净收入 | 罚分 | 主要约束 |
|------|------:|-----:|---------|
| D001 | 11,039.21 | 300 | 每日连续休息 8h（深圳范围） |
| D002 | 18,909.98 | 1,600 | 每日连续休息 4h + 无成交日 |
| D003 | 829.60 | 0 | 月度空驶 ≤100km |
| D004 | 15,023.86 | 0 | 首单 ≤12:00 + 每日 ≤3 单 |
| D005 | 17,051.64 | 0 | 装卸距离 ≤100km |
| D006 | 19,079.24 | 200 | 每日连续休息 5h + 完全不出车日 |
| D007 | 19,720.82 | 0 | 23-04 不接单 + 无成交日 |
| D008 | 21,235.62 | 2,800 | 平日连续休息 4h + 完全不出车日 |
| D009 | 10,526.21 | 900 | 每日 23:00 前到家 + 熟货 240646 |
| D010 | 19,353.10 | 6,270 | 家事 3/10-3/13 + 每日休息 3h + 必访点 |

**总计：净收入 152,769.28，罚分 12,070，失败司机 0，token 0。耗时 ~143s。**

### Qwen3.5-Flash 集成测试结论（2026-06-06）

已完成代码集成（`planner.py` + `llm_helper.py` + `preference_rules.py`）：

- `preference_hints()`：偏好结构化，每 driver 调用一次并缓存。~1100 token/driver。**正常工作**。
- `rank_cargos()`：**已禁用**（代码保留）。每次 ~5000 reasoning token，模型总是确认确定性最高分，不改变决策。
- `suggest_decision()`：**保守触发保留**（5 步冷却期，高风险 AND 分数接近才触发）。每次 ~750 token。
- `apply_qwen_hints()`：偏好解析后调用，只能收紧约束不能放松。
- 安全降级：模型调用失败时完全回退确定性逻辑。

**完整 31 天 Qwen 仿真结果（max_reviews=10）**：

| 指标 | 确定性基线 | Qwen 模式 | 变化 |
|------|----------:|----------:|-----:|
| 净收入 | 152,769 | 142,895 | **-9,874** |
| 罚分 | 12,070 | 12,070 | 0 |
| Token | 0 | 39,241 | +39,241 |
| 耗时 | 143s | 371s | +228s |
| D009 净收入 | 10,526 | 423 | **-10,103** |

**问题根因**：

1. suggest_decision 无价值：模型看到 deterministic_score 后直接选最高分，与确定性选择 100% 相同。
2. 移除分数后有害：不给分数时模型选择低分候选，被安全检查拒绝但浪费 token。
3. D009 严重回退：模型在 home-night 场景做出有害决策。
4. rank_cargos token 过高：qwen3.5-flash 是推理模型，每次 ~5000 reasoning token。

**结论**：Qwen3.5-Flash 当前不适合完整集成。需要领域特化 prompt 或换用非推理模型（qwen-turbo/qwen-plus）。

### 已知限制

1. **D010 家事迟到**（3,570 罚分）：家事偏好在 3/10 10:00 才可见，但司机在之前已接长单。这是偏好可见性窗口的结构性限制，Agent 层面无法完全规避。
2. **D002/D008 休息违规**（8/7 天）：跨日休息被长单切碎，需要更智能的跨日休息预测。
3. **Qwen 不适合当前场景**：suggest_decision 无差异化，rank_cargos token 过高。需要领域特化 prompt 或非推理模型。
4. **market_heat 跨步记忆**：当前只在当前决策步内累积，不做跨步持久化。

### 当前高优先级事项

1. **Qwen 领域特化 prompt**：为 D009 home-night、D010 family、D002/D008 rest 等高风险场景设计专用 prompt。
2. **换用非推理模型**：qwen3.5-flash 是推理模型，每次 ~5000 reasoning token。尝试 qwen-turbo 或 qwen-plus。
3. **D002/D008 休息违规**：需要跨日休息预测或更早的休息触发。
4. **内置进度显示增强**：当前 heartbeat 只显示 driver/step/sim/action；需要补成评测进度面板（当前仿真日期、已完成司机数、阶段摘要）。

### 当前低优先级事项

1. D003 空驶限额内的收益优化。
2. market_heat 跨步记忆持久化。
3. Qwen 约束验证模式（验证确定性选择，而非在候选中选择）。

## 16. Hybrid Agent 切换门槛与实施计划

不要把确定性规则调参变成无限循环。当前项目已经满足进入 Hybrid Agent 的基础门槛，后续重点是验证和收紧 Qwen 的受控参与，而不是回退到无限手写规则。

- 完整 31 天仿真可以跑完。
- 无 `validation_error`。
- 无仿真崩溃。
- 10 名司机动作日志非空。
- 无 API key 时确定性策略仍可独立运行。
- 主要罚分点已经可定位到规则类别和动作日志区间。
- 本地候选生成、动作校验、异常 fallback 已稳定。

不要求所有罚分清零后才进入 Hybrid。D009 home-night、D010 家事、连续休息、必访点等问题正是 Qwen 应参与理解和复审的重点。

**重要更新（2026-06-06）**：Qwen3.5-Flash 完整集成测试已完成。结论是该模型当前不适合直接接入主流程：
- suggest_decision 看到确定性分数后直接选最高分（无价值）
- 移除分数后模型乱选（有害）
- rank_cargos 每次 ~5000 reasoning token，总量失控
- 完整 31 天 Qwen 模式净收入比确定性低 9,874

### Hybrid Agent 下一步方向

基于测试结论，后续 Hybrid Agent 应采取以下新策略：

1. **换用非推理模型**：`qwen-turbo` 或 `qwen-plus` 替代 `qwen3.5-flash`，减少 reasoning token。
2. **领域特化 prompt**：不为通用"选择最佳动作"，而是为具体约束场景设计 prompt（如"司机必须在 23:00 前到家，当前 18:00，距家 100km，是否接单？"）。
3. **约束验证而非选择**：让 Qwen 验证确定性选择是否违反隐含约束，而非在候选中选择。
4. **预计算风险场景**：为 D009 home-night、D010 family、D002/D008 rest 预定义触发规则，Qwen 只在规则边界做微调。
5. **只在真正不确定时介入**：确定性分数差距 <5% 且有多个合理选择时才调用。
6. **冻结确定性底座**：只修崩溃、非法动作、硬约束绕过，不再做大范围纯规则重构。

### 原有 Hybrid Agent 计划（供参考）

1. 接入偏好结构化：Qwen 输出 JSON hints，本地只允许新增或收紧约束。（已完成，正常工作）
2. 接入候选复审：本地先生成合法候选，Qwen 只返回候选 index。（已实现但无差异化价值）
3. 本地复核：模型选择必须通过本地校验，否则回退确定性首选。（已完成）
4. 控制调用：用 `AGENT_QWEN_MAX_REVIEWS` 限制复审次数。（已完成，默认 10）
5. 对比评测：分别跑不开 Qwen 和启用 Qwen 的短测与 31 天完整评测。（已完成）

Hybrid Agent 约束：

- Qwen3.5-Flash 是基座模型，但不是自由动作生成器。
- 模型调用只能通过 `SimulationApiPort.model_chat_completion`。
- 请求体显式使用 `model: "qwen3.5-flash"`（或更换为 `qwen-turbo`/`qwen-plus`）。
- 不给模型原始全量数据文件。
- 不提交真实 API key、本地 config 或 results。
- 模型失败、超时、坏 JSON、候选越界时必须无损降级。
