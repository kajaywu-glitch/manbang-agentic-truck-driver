# 最终冲刺与泛化方案

最后更新：2026-06-07 00:25 +08:00

## 1. 新数据集结论

官方 `demo_docs_release_20260529.zip` 已核对：

- 货源共 500,000 条、31 个品类，公开文件与当前工作区数据哈希一致。
- `query_cargo` 新增 `k` 参数，默认 100、上限 600；扫描时间仍为 `ceil(返回条数 / 10)` 分钟。
- 本地只公开 D001、D002，正式评测还会使用未公开的 D003、D004。
- 新偏好包含口语化禁货、城市起终点限制、每日静默窗口、整日停驶、指定区域接单天数、临时日期窗口、指定地点停留和多站顺序任务。
- `get_driver_status` 只在规则可见时返回偏好，并返回 `content`、`penalty_amount`、`penalty_cap`。

旧数据集的 `154,296.52 / 8,355 / 25,721` 只能作为旧版工程回归记录，不能作为新数据集成绩基线。

## 2. 当前阻塞

现有 `parse_preferences()` 对新公开偏好的覆盖严重不足：

- D001 仅识别每日连续休息 8 小时。
- D001 的机械设备禁运、惠州起终点禁运、3 个整日停驶、3 月 4 至 5 日深圳回避均未识别。
- D002 的 0 至 6 点静默、蔬菜禁运、增城接单日目标、55km 赴装货限制、2 个整日停驶、3 月 12 日停留任务、3 月 31 日多站行程均未识别。

问题不是少几个正则，而是当前策略没有统一表达“规则类型、时间窗口、地点、累计进度、罚款和动作影响”的约束中间表示。

## 3. 最终架构

### 3.1 运行时偏好编译器

把每条当前可见偏好编译为通用 `ConstraintSpec`，至少包含：

- `kind`：cargo_forbid、region_forbid、quiet_window、daily_rest、off_days、pickup_limit、required_region_days、appointment、route_sequence、visit、home_stay、unknown。
- `scope`：单订单、每日、自然月、一次性事件。
- `penalty_amount`、`penalty_cap`、是否无上限。
- 时间：每日窗口、绝对日期/截止时间、持续时间。
- 地理：坐标、半径、城市/区县关键词、起点或终点作用域。
- 进度：目标次数/天数、当前完成量、剩余量。
- 动作影响：take_order、wait、reposition 中哪些动作会满足或违反规则。
- `confidence` 与原始文本。

先用确定性解析覆盖常见结构；无法可靠解析时，只在偏好首次出现或文本变化时调用一次 Qwen 生成严格 JSON，校验后缓存。Qwen 不直接生成最终动作。

### 3.2 约束日程器

- 每日静默和连续休息生成不可侵占时间块。
- 整日停驶在月初优先安排到低机会日，不等月末补作业。
- 区域接单天数维护已完成日期集合和剩余可用天数。
- 临时任务拆成有顺序的 waypoint、到达截止和最短停留。
- 新偏好出现时立即重算日程；未出现前不得预知。

### 3.3 罚款感知候选投影

对接单、等待、空驶统一计算：

```text
utility =
  immediate_net
  + destination_option_value
  + constraint_progress_value
  - expected_penalty_delta
  - future_schedule_loss
  - query_and_travel_opportunity_cost
```

无上限罚款、一次 5,000/10,000 元任务和每日高额规则优先视为硬约束。只有可避免罚款低于明确机会收益时，才允许经济性权衡。

### 3.4 自适应货源查询

- 常规决策使用 `k=100`，保持 10 分钟扫描成本。
- 目标区域/目标品类稀缺且高额任务临近时，可提高至 200 或 300。
- `k=600` 只用于极少数临近截止的定向搜索。
- 候选按起终点城市、品类、赴装货距离、完成时间和未来日程过滤。
- 市场热度只来自本次运行已观察货源，不能预统计并硬编码公开全量数据。

## 4. 泛化原则

1. 决策代码不出现 D001/D002/D003/D004 分支。
2. 不读取或复制 `cargo_dataset.jsonl`、`drivers.json`。
3. 不把公开城市名、货物名或官方原句写成专属逻辑；它们只能是运行时参数。
4. 为偏好解析写同义改写测试，而不是只测官方原句。
5. 未知规则进入保守路径：避免长单、保留时间余量、必要时 Qwen 解析，不静默忽略。
6. 旧版专用 `FamilyTask`、`HomeNightRule` 应逐步迁移到通用 appointment/route/quiet/visit 组合。

## 5. 最终验收

- 所有运行时偏好都被编译，未知规则数为 0；低置信规则有明确保守处理。
- 禁货、禁区域、赴装货距离、每日静默、整日停驶、指定区域日数和临时任务均有独立测试。
- 高额一次性任务和无上限罚款零违规。
- 每日高额规则尽量零违规；如发生，日志给出不可避免原因。
- failed=0、validation_error=null、模型失败可降级。
- 模型调用以“偏好首次编译 + 少量高风险复审”为主，不做每步自由推理。
- 在满足约束后，再以净收入、单位时间净收益和目的地机会价值排序货源。

最终提交以新数据集结果为准，旧版总分目标不再作为保留或回退依据。
